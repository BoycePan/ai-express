# 开发贡献指南 📝

感谢你对水墨快递项目的关注！本文档将帮助你了解项目的开发规范和贡献流程。

## 🚀 开发前准备

### 1. 克隆项目

```bash
git clone <repository-url>
cd testSend
```

### 2. 安装依赖

```bash
pnpm install
```

### 3. 启动开发服务器

```bash
pnpm dev
```

## 📋 开发规范

### 代码风格

本项目使用 **[@antfu/eslint-config](https://github.com/antfu/eslint-config)** 来保证代码质量和统一的代码风格。

这是一个开箱即用的 ESLint 配置方案，内置代码格式化功能，支持 Vue、TypeScript、JSON、Markdown 等多种文件类型。

#### 运行代码检查

```bash
# 检查代码质量问题
pnpm lint:check

# 检查并自动修复（包含代码格式化）
pnpm lint
```

### Git 提交规范

本项目采用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/) 规范。

#### Commit Message 格式

```
<type>(<scope>): <subject>

[optional body]

[optional footer]
```

#### Type 类型说明

| Type     | 说明                                   | 示例                                |
| -------- | -------------------------------------- | ----------------------------------- |
| feat     | 新功能                                 | `feat(order): 添加订单筛选功能`     |
| fix      | 修复 Bug                               | `fix(login): 修复验证码不显示问题`  |
| docs     | 文档变更                               | `docs(readme): 更新安装说明`        |
| style    | 代码格式（不影响代码运行的变动）       | `style: 统一代码缩进`               |
| refactor | 重构（既不是新增功能，也不是修复 Bug） | `refactor(user): 优化用户登录逻辑`  |
| perf     | 性能优化                               | `perf(list): 优化订单列表渲染性能`  |
| test     | 增加测试                               | `test(order): 添加订单创建单元测试` |
| build    | 构建过程或辅助工具的变动               | `build: 升级 vite 到 5.0`           |
| ci       | CI 配置文件和脚本的变动                | `ci: 添加自动部署配置`              |
| chore    | 其他不修改 src 或 test 的变动          | `chore: 更新依赖包`                 |
| revert   | 回退                                   | `revert: 撤销上一次提交`            |

#### 使用 Commitizen (推荐)

项目集成了 Commitizen，提供交互式的 commit message 生成：

```bash
# 使用 commitizen 提交代码
pnpm commit
```

然后根据提示选择 type、输入 scope 和 subject 即可。

#### 手动编写 Commit Message

如果你更喜欢手动编写 commit message：

```bash
git add .
git commit -m "feat(order): 添加订单导出功能"
```

**注意**：提交时会自动触发以下检查：

1. **pre-commit hook**：自动格式化和检查暂存区的代码
2. **commit-msg hook**：验证 commit message 是否符合规范

如果检查未通过，提交将被阻止，你需要修复问题后重新提交。

## 🔄 开发工作流

### 1. 创建功能分支

```bash
git checkout -b feature/your-feature-name
```

### 2. 开发功能

- 编写代码
- 运行 `pnpm dev` 实时预览
- 运行 `pnpm lint` 检查代码质量

### 3. 提交代码

```bash
# 添加文件到暂存区
git add .

# 使用 commitizen 提交（推荐）
pnpm commit

# 或手动提交
git commit -m "feat: your feature description"
```

### 4. 推送代码

```bash
git push origin feature/your-feature-name
```

### 5. 创建 Pull Request

在 GitHub/GitLab 上创建 Pull Request，等待代码审查。

## 🛠️ 常见问题

### Q: 提交被阻止了怎么办？

A: 检查以下几点：

1. 运行 `pnpm lint` 修复代码问题
2. 确保 commit message 符合规范
3. 查看错误信息，根据提示修复

### Q: 如何跳过 Git Hooks？

A: **不推荐**跳过 hooks，但在紧急情况下可以使用：

```bash
git commit --no-verify -m "your message"
```

### Q: 如何统一格式化整个项目？

A: 运行以下命令：

```bash
pnpm lint
```

## 📚 相关资源

- [Vue 3 文档](https://cn.vuejs.org/)
- [TypeScript 文档](https://www.typescriptlang.org/zh/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Conventional Commits](https://www.conventionalcommits.org/zh-hans/)
- [@antfu/eslint-config](https://github.com/antfu/eslint-config)

## 💡 开发建议

1. **提交频率**：保持小而频繁的提交，每个提交只做一件事
2. **分支管理**：每个功能或修复使用独立的分支
3. **代码审查**：提交 PR 前自己先审查一遍代码
4. **测试验证**：确保修改不会影响现有功能
5. **文档更新**：如有必要，及时更新相关文档

---

再次感谢你的贡献！🎉
